// WAP to check whether tewo numberd are equal or not. Print 0 for not equal and 1 for equal.
#include <iostream>
using namespace std;
int main()
{
    int a, b;
    cin >> a >> b;
    cout << (a == b) ? 1 : 0;
}